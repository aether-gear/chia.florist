# Chia Florist — control-panel Environment Variables
# Copy to .env and fill in real values.
#
# NOTE: All VITE_* variables are embedded into the build at compile time.
#       Do NOT put secrets here — they will be visible in the browser bundle.

# --- API Base URL --- #
SERVICE_CORE_API_URL=http://127.0.0.1:7129/api/core